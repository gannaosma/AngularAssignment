import { CreditformatPipe } from './creditformat.pipe';

describe('CreditformatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditformatPipe();
    expect(pipe).toBeTruthy();
  });
});
