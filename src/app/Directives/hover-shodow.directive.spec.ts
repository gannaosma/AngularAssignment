import { HoverShodowDirective } from './hover-shodow.directive';

describe('HoverShodowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverShodowDirective();
    expect(directive).toBeTruthy();
  });
});
