import { CreditFormatDirective } from './credit-format.directive';

describe('CreditFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditFormatDirective();
    expect(directive).toBeTruthy();
  });
});
